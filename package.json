{
  "name": "orchestra-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for the Orchestra Hub workflow orchestration system.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "dev:api": "tsx scripts/dev-api.ts",
    "dev:worker": "tsx scripts/dev-worker.ts",
    "dev:scheduler": "tsx scripts/dev-scheduler.ts",
    "seed": "tsx scripts/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.5",
    "typescript": "^5.6.3"
  }
}
